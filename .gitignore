# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local

# local Databricks U2M OAuth token cache (dev only — never commit)
.databricks-oauth-cache.json

# typescript
*.tsbuildinfo
next-env.d.ts

# reference projects (~1.1 GB with their node_modules) are runnable references,
# never part of the build/deploy — excluded so `databricks sync` skips them.
/docs/references
