// Shared user-facing copy (pt-BR). Centralizing recurring strings keeps the
// voice consistent across screens — see CONTEXT.md for the product vocabulary.

/** Badge shown while a screen is served by the mock dataset (DATA_SOURCE=mock). */
export const MOCK_DATA_LABEL = "Dados simulados (pré-visualização)";
